{
  "name": "0g-python-sdk-docs",
  "version": "1.0.0",
  "description": "0G Python SDK Documentation",
  "scripts": {
    "dev": "mint dev",
    "build": "mint export && unzip -o export.zip -d out"
  },
  "devDependencies": {
    "@mintlify/cli": "latest"
  }
}
